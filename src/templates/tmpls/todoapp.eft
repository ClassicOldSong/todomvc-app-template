>section.todoapp
	>header.header
		>h1
			.todos
		>input.new-todo#input
			#placeholder = What needs to be done?
			#autofocus
			%value = {{input}}
			@keypress.prevent.13 = addTodo:{{input}}
	-main
	-footer
