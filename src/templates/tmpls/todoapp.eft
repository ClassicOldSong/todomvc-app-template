>section.todoapp
	>header.header
		>h1
			.todos
		>input.new-todo#input
			#placeholder = What needs to be done?
			#autofocus
			%value = {{input}}
			@keypress.13.prevent = addTodo:{{input}}
	-main
	-footer
